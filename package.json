{
  "name": "dns-spf-lookup-tool",
  "version": "1.0.0",
  "description": "A web application for DNS SPF record lookup and analysis",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["dns", "spf", "lookup", "security"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^4.18.2",
    "ip-range-check": "^0.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
